namespace Server.Models;

public enum PointDirection
{
    Top = 0,      // Hacia arriba (Jugador 1)
    Right = 1,    // Hacia derecha (Jugador 2)
    Bottom = 2,   // Hacia abajo (Jugador 3)
    Left = 3      // Hacia izquierda (Jugador 4)
}

